apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-agg-pvc
  labels:
    app.kubernetes.io/name: vllm-agg-pvc
spec:
  accessModes:
    - "ReadWriteMany"
  storageClassName: azurefile
  resources:
    requests:
      storage: 100Gi
