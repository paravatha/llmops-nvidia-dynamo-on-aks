apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-disagg-planner-pvc
  labels:
    app.kubernetes.io/name: vllm-disagg-planner-pvc
spec:
  accessModes:
    - "ReadWriteMany"
  storageClassName: azurefile
  resources:
    requests:
      storage: 50Gi
