apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-disagg-router-pvc
  labels:
    app.kubernetes.io/name: vllm-disagg-router-pvc
spec:
  accessModes:
    - "ReadWriteMany"
  storageClassName: azurefile
  resources:
    requests:
      storage: 100Gi
